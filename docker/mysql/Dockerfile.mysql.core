FROM hahlabs/ubuntu:core
LABEL version.hahlabs-mysql="core-00.09.00"

COPY --chown=hahlabs:hahlabs files/. files/
USER root
RUN  apt -y update && apt -y upgrade && apt -y install zip cron \
     && files/setup-mysql.sh \
     && mysql  < files/hahlabs-user.sql  \
     && cp files/startup.core.sh /home/hahlabs/startup.sh \
     && rm -rf files 
     
USER hahlabs
EXPOSE 3306 
# Enables the service on docker run container
ENTRYPOINT sudo ./startup.sh 