build shams first before alpha
This docker container should have
1. nginx runs on port 8201 <default nginx site>
2. php 8.2
3. GCP rpc

Match set-env.sh/RELEASE with Dockerfile.laravel.alpha/version.hahlabs-laravel